---
id: "pr-triage-2026-06-03"
title: "PR Triage — Open PR Analysis and Merge Plan"
status: "done"
created: "2026-06-04"
updated: "2026-06-04"
owner: "system"
tags: []
related_wiki: []
priority: "medium"
---

## Objective
Audit the 9 open PRs targeting `dev`, identify duplicates, assess value, detect potential merge conflicts, and produce a final merge-vs-close decision plan with an ordered merge sequence.

## Plan


## Progress Log
- [2026-06-04T00:26:44.309Z] Created initiative via mdocs command
- ## Triage complete — 9 PRs analyzed, 3 duplicate clusters identified, 4 merge + 5 close decisions produced.
- **MERGE #139** — strict superset. Only one that batches `admin/categories/+page.server.ts`. Correct `Awaited<T[K]>` batch return type. Pre-merge cleanup needed: remove unused `LeanMoveRaw` (src/routes/+page.server.ts:2) and `AdminLeanMoveRaw` (src/routes/admin/+page.server.ts:2) imports; refresh stale `featuredMoveRaw (wrapped in array by destructuring)` comment (src/lib/server/routes/+page.server.security.spec.ts:105).
- **CLOSE #132** — superseded. **Has path-case bug**: commit tree contains BOTH `.Jules/bolt.md` (original) and `.jules/bolt.md` (new). On macOS APFS the new file overwrites the original; on Linux/Cloudflare build two files coexist. Also uses wrong `Promise<U>` batch return type and unnecessary `as any` cast.
- **CLOSE #133** — superseded. Cleanest diff of the four but missing the categories page batching (3-line gap).
- **CLOSE #134** — superseded. Has best test-comment cleanup but leaves two unused imports that will fail `npm run lint`.
- **MERGE #138** — canonical. **CSP is functionally correct**: `style-src` includes `'unsafe-inline'` (required by Svelte transitions per SvelteKit docs). Adds HSTS `preload`, defensive `base-uri`/`form-action`/`frame-ancestors`/`upgrade-insecure-requests`. Uses idiomatic `event.setHeaders()` logout pattern.
- **CLOSE #135** — superseded AND has a critical bug. **CSP at svelte.config.js:21 specifies `style-src: ["'self'", 'https://fonts.googleapis.com']` WITHOUT `'unsafe-inline'`, which would silently break the 5 Svelte transition call sites** in Header.svelte (lines 220, 285, 321), admin/moves/[id]/edit/+page.svelte:96, and admin/categories/+page.svelte:227 in production. Unique value (admin guard prefix tightening, X-Permitted-Cross-Domain-Policies, expanded Permissions-Policy) is small and can be ported as a follow-up if desired.
- **MERGE #136** — strict superset. Includes all of #131's loading-state changes plus `aria-invalid` a11y, input `disabled` hardening, and row-actions conflict prevention (prevents delete/edit column from rendering alongside the inline edit form).
- **CLOSE #131** — superseded by #136.
- **MERGE #137** — search input UX. No duplicates. Touches Header.svelte and SearchBar.svelte only; no file conflicts with any other surviving PR.
- ## Triage complete — final report delivered to user. Awaiting confirmation to mark initiative as `done`.
- 9-PR inventory with cluster detection (3 duplicate clusters + 1 standalone)
- Code-level diff comparison for every duplicate pair
- Critical bug found: #135 CSP would break Svelte transitions in production
- Critical bug found: #132 path-case mismatch (`.jules/` vs `.Jules/`)
- Final merge/close decision table with reasoning
- Recommended merge order (disjoint file sets, any order safe)
- Wiki entry: `process/agent-pr-spawn-pattern.md` — operational playbook for the recurring duplication pattern, including recommended guardrails for Guardian and Bolt/Sentinel/Palette task dispatch.
- ## Plan executed — 2026-06-04
- #132 — superseded by #139 (had path-case regression `.jules/` vs `.Jules/`, wrong batch return type)
- #133 — superseded by #139 (missing categories page batching)
- #134 — superseded by #139 (unused imports would fail lint)
- #135 — superseded by #138 (CSP `style-src` without `'unsafe-inline'` would have broken 5 Svelte transitions in production)
- #131 — superseded by #136 (strict subset)
- #140 — ⚡ Bolt: Use db.batch() for parallel queries (created 00:29:35, branched from pre-merge dev). Pure duplicate of #139; closed.
- #141 — 🛡️ Sentinel: security headers and logout hardening (created 00:34:44, branched from pre-merge dev). Mostly duplicate of #138; closed. One unique value (admin guard path tightening) flagged as optional follow-up.
- `npm run lint` — passes. 16 warnings, all in mdocs/ and .Jules/ (not code). No warnings in any of the 14 source files my PRs touched.
- `npm run test:unit --project=server` — 16 files, 85 tests passing.
- `npm run test:unit --project=client` — 6 files, 30 tests passing.
- **Total: 115 tests passing** (matches the count #138 cited in its description).
- `npm run check` — 7 errors remain in test specs (`RequestEvent` type incompatibility in `moves.security.spec.ts` and `categories.security.spec.ts`).
- 4 PRs merged, 7 PRs closed (5 from the original plan + 2 that appeared mid-execution)
- 0 open duplicate PRs remaining
- All tests pass; lint is clean for code; 7 pre-existing `npm run check` errors documented
- One piece of unique value flagged as optional follow-up: admin guard path tightening from #141 (3-line change in src/hooks.server.ts)
- [2026-06-04T00:58:08.125Z] Marked done via mdocs command

## Artifacts
